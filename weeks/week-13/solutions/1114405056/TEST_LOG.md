# TEST LOG

## Red

- 初始狀態沒有任何程式、測試、報告檔案。
- 先依作業要求建立 `task1_grouped_bar.py`、`task2_zipcode_heatmap.py` 與 `tests/`。

## Green

- 已完成 `load_year()`、`get_top_depts()`、`load_county_counts()`、`get_top_counties()`。
- 已補上 `matplotlib` 依賴並可輸出 `output/task1.png`、`output/task2.png`。
- 已建立 10 個單元測試，覆蓋 Task 1 與 Task 2 的核心規格。