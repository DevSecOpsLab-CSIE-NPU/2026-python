## 我問 AI 什麼
請幫我拆 `sum_of_gcd(n)` 的 3 個 test case，至少包含 1 個 edge case，並依 TDD 流程完成 0603 題目。

## AI 給了什麼
AI 幫我整理出 `n=2`、`n=10`、`n=1` 這 3 個測試案例，並協助我完成測試與實作流程。

## 我改了什麼
我有自己確認 `n=1` 是 edge case，結果應為 0；另外我補上 `gcd.py`，用雙迴圈搭配 `math.gcd` 正確計算所有 `i < j` 的總和，最後重新跑測試確認全綠。
