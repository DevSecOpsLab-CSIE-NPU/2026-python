## 我問 AI 什麼
請幫我把 UVA 11417 GCD 拆成至少 3 個 test case，包含基本案例和 edge case，並補出對應的實作。

## AI 給了什麼
AI 提供了 n=2、n=10、n=1 的測試方向，也給了用雙層迴圈累加 gcd 的實作做法。

## 我改了什麼
我把測試整理成 `n=2`、`n=10`、`n=1` 的必要案例，再額外補一個 `n=3`；實作部分我加上 `n < 1` 時丟出 `ValueError("n must be >= 1")`，讓函式更完整。
