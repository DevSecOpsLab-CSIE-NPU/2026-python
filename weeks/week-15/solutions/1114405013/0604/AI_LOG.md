## 我問 AI 什麼
請幫我完成 count_squares(a, b) 這題的 unittest 測試、實作流程與 AI_LOG。

## AI 給了什麼
提供了基本案例、edge case 與例外案例的測試方向，並用整數平方根實作區間內完全平方數的計數。

## 我改了什麼
我確認題目要求一定要有 `a > b` 時丟出 `ValueError` 的例外測試，並依照 TDD 先跑紅燈後才建立 `square_counter.py`。
