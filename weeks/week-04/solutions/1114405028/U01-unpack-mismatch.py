# U1. 解包失敗的原因：變數數量 ≠ 元素數量（1.1）

p = (4, 5)  # tuple 包含 2 個元素

# x, y, z = p  # ValueError：右邊只有 2 個元素，但左邊要求 3 個變數
                # Python 解包時會嚴格核對數量，不符即拋出 ValueError
                # 解決方式：用 _ 忽略不需要的元素，或使用 *var 收集剩餘
