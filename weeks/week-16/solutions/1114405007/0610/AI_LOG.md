## 我問 AI 什麼

「閱讀 week-16/in_class/0610-starter/README.md，把題目拆成至少 3 個 test case（含 edge case），並寫進 test_digit_root.py；再完成 digit_root.py 讓測試全綠。」

## AI 給了什麼

AI 先提供 6 個測試案例（一般、edge、例外），再產生 `test_digit_root.py` 與 `digit_root.py` 的實作，並協助執行 `unittest`。

## 我改了什麼

我提供並更新了正確的 git 身分資訊（`user.name`、`user.email`），並確認例外訊息必須精準比對 `n must be >= 1`；另外我檢查了紅燈（缺少模組）到綠燈（6 tests OK）的完整流程，確保提交順序符合先測試後實作。
