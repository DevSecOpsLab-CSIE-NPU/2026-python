# AI_LoG.md

## 我問 AI 什麼

請幫我根據 week-16 的數字根題目設計至少 3 個 unittest 測試案例，必須包含基本案例、edge case 與 `n < 1` 的例外案例。

## AI 給了什麼

AI 建議用一個多位數、單一位數與例外案例來覆蓋核心行為，並提醒錯誤訊息要精準比對 `n must be >= 1`。

## 我改了什麼

我把測試補成 4 個案例，除了基本案例、edge case 和例外案例外，再加一個重複降階案例，確認 `digit_root` 會反覆相加直到剩下 base 以內的一位數。另把實作固定為 base 8，符合我這次的學號對照結果。
