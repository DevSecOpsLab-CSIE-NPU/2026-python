# AI_LOG.md - 0610 數字根限時演練

## 🤖 提示詞記錄 (逐字貼上)

### 階段 1：拆題與規格確認
> 我：請幫我分析「數字根」這個題目。函式簽名為 `digit_root(n: int) -> int`，輸入範圍 1 到 20 億，若 n < 1 需拋出 `ValueError("n must be >= 1")`。請列出所有必要的測試案例。

### 階段 2：測試程式碼生成 (TDD Red)
> 我：請根據上述規格，幫我寫一個 `test_digit_root.py`。我打算先跑紅燈測試，請確保包含基本案例、大數邊界以及負數例外處理。

### 階段 3：實作程式碼生成 (TDD Green)
> 我：測試已準備好。請提供 `digit_root.py` 的實作，使用 `while` 迴圈來反覆計算數位和。

---

## 🛠 我改了什麼 (審查紀錄)

1. **修正匯入邏輯**：AI 給的測試程式碼原本使用相對匯入，但在執行 `python -m unittest` 時會出錯。我改回了標準的 `from digit_root import digit_root` 並調整執行目錄。
2. **優化大數測試**：額外增加了 $n=2,000,000,000$ 的測試，確保在大數輸入下邏輯依然穩健。
3. **編碼宣告**：手動在檔案開頭加入 `# -*- coding: utf-8 -*-`，確保在 Windows 終端機不會出現亂碼。

