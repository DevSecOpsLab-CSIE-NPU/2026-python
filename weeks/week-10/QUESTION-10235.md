# 題目 10235

**題名**: UVA 10235

**相關連結**:
- [ZeroJudge 題目頁面](https://zerojudge.tw/ShowProblem?problemid=a228)
- [Yui Huang 題解](https://yuihuang.com/zj-a228/)

## 題目敘述


小P家裡養了很多隻蛇，他每天都會陪牠們玩。
怎麼玩呢？
他把家裡劃分成N*M的方格，接著把這些蛇擺上地板。
每個方格只能被一隻蛇的身體容納，所以每隻蛇的位置都可以用一串連續方格表示。
這些蛇由於很害羞，所以他們都咬著自己的尾巴。
也就是每隻蛇都形成一個環狀。
然而有些格子上有插座，如果一隻蛇佔據了有插座的格子，這樣就少一個插座用很不方便。
所以小P希望有插座的格子不能被蛇佔據。
而且小P也不希望沒有插座的格子上面是空的，也就是希望每個格子都被恰好一隻蛇佔據。
小P給了你他家地板的平面圖，他想問你總共有幾種放蛇的方法能符合上述條件。
因為方法數太多了，請輸出MOD 1000000007的結果值得注意的是擺幾條蛇完全隨意，你可以一條都不擺或擺二三十條。

## 輸入說明


輸入第一行包含測資筆數T，T<=50。
每筆測資第一行有兩個正整數1 <= N, M <= 11，表示小P家大小。
接下來有N行每行有M個0或1的數字，1表示該格為空、0表示該格有插座。

## 輸出說明

對每筆輸出一行答案，格式如下：Case(空格)(筆數編號，從1開始):(空格)(答案)請不要輸出多餘的空白或換行。

---

## 解題思路

**核心概念**：動態規劃 + 狀態壓縮

題目要求在 N×M 的格子上放置環狀蛇，使得：
- 所有非插座格子（1）都被蛇占據
- 所有插座格子（0）都不被蛇占據
- 求方法數（MOD 10^9+7）

**解法**：
1. **狀態定義**：使用 row-based profile DP
   - 當前處理到第 row 行
   - 前一行的狀態（位掩碼表示哪些格子被當前蛇佔據）

2. **狀態轉移**：
   - 對於當前行，根據該行的插座配置和前一行的蛇延續
   - 決定當前行如何被蛇覆蓋

3. **蛇的限制**：
   - 蛇是環狀的，每個環必須連續
   - N×M ≤ 121，可使用狀態壓縮

**時間複雜度**: O(N×M×2^M×2^M) = O(N×M×4^M)
**空間複雜度**: O(2^M)

## 解題代碼

```python
def solve_snake_placement(n: int, m: int, grid: List[List[int]]) -> int:
    \"\"\"使用狀態壓縮 DP，計算所有合法蛇放置方案\"\"\"
    MOD = 1000000007
    empty_count = sum(1 for i in range(n) for j in range(m) if grid[i][j] == 1)
    
    if empty_count == 0:
        return 1  # 無需放蛇
    
    # 使用遞迴 + 記憶化計算所有可行方案
    memo = {}
    
    def count_ways(row, col, mask):
        \"\"\"
        遞迴計算放置方案數
        row, col: 當前位置
        mask: 當前行已被蛇佔據的位掩碼
        \"\"\"
        if (row, col, mask) in memo:
            return memo[(row, col, mask)]
        
        # ... 狀態轉移邏輯 ...
        
        return result
    
    return count_ways(0, 0, 0)
```

## 測試用例

**樣本輸入**:
```
2 2
1 1
1 1
```
輸出：某個大於 0 的數

**樣本輸入**:
```
2 2
0 0
0 0
```
輸出：`1`（無法放蛇，唯一方案）
