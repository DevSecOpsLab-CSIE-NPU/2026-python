主要改進：
1. 修正 comb_sum 計算錯誤：原始版的組合數更新公式有誤，導致計算結果不正確。優化版使用正確公式 C(t,i) = C(t,i-1) * (t-i+1) // i，並在正確時機累加（先更新再加入總和）。
2. 二分搜尋替代線性搜尋：原始版用 for t in range(1, 64) 線性搜尋，優化版用二分搜尋，從最多 63 次降為約 6 次（comb_sum 隨 t 單調遞增，適用二分搜尋）。
原始版 comb_sum 錯誤示範（t=3, k=2）：
- 原始版：返回 2（錯誤，正確應為 C(3,1)+C(3,2) = 3+3 = 6）
- 優化版：正確返回 6