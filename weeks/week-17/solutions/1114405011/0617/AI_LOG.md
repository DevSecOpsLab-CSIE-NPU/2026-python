# AI 協作紀錄 (0617 預演)

## 量測摘要 (n=200000, repeat=5)
1. **linear 平均約**：0.00946270 秒
2. **binary 平均約**：0.00000486 秒
3. **sort 平均約**：0.05185128 秒
---

## AI 反問我什麼 / 我怎麼回答

| AI 反問的規格問題 / 階段閘門 | 我的決定 / 回答 | 檢查表狀態 |
| :--- | :--- | :--- |
| **【資訊檢查表】** 對 `timeit` 的例外規則，是否只要求 `repeat < 1` 時 `raise ValueError`？ | 是，只處理 `repeat < 1`，其餘例外由原函式正常拋出。 | `✅例外` |
| **【資訊檢查表】** `timeit` 的 edge case 你要我納入哪些？ | 1. repeat=1 正常記錄<br>2. 副作用函式老實執行 repeat 次<br>3. records 跨呼叫持續累積不重設。 | `✅edge` |
| **【資訊檢查表】** `binary_search` 收到未排序 data 時，你要哪種行為？ | 選擇不主動檢查效能（不保證結果），只寫在 docstring 中明確註明前置條件。 | `✅邊界` |
| **【資訊檢查表】** 紅燈驗收標準要用哪個？ | 先只有 `test_timing.py` 全紅且訊息合理，`search.py` 採輕量評估。 | `✅驗收` |
| **【資訊檢查表】** README 的效能評估要寫短版還是含量測數字表格？ | 依教案要求寫短版 2-3 句即可。 | `✅簽名` |
| **【階段閘門 1】** 為什麼要用 `raise ValueError` 而不是 `assert`？ | 因為 assert 在 -O 最佳化模式下會被忽略而失效，raise 才能確保任何環境都執行驗證。 | `✅全部通過` |
| **【階段閘門 2】** 進入綠燈後要做哪些實作？ | 同意實作 `timeit`、`search` 並完成效能評估。 | `✅全部通過` |