# AI 協作開發紀錄 (AI_LOG.md)

## 階段一：裝飾器規格與邊界條件討論

### 1. 我問了什麼 (User Input)
* 詢問 `timeit` 裝飾器如何同時支援 `@timeit` 與 `@timeit(repeat=N)` 的語法。
* 確認 `repeat < 1` 與輸入非整數（如 float）時的例外處理機制。
* 釐清當被裝飾函式拋出例外時，裝飾器該如何應對。

### 2. AI 給了什麼 (AI Output)
* 提出利用內部判斷第一個參數是否為可呼叫物件（Callable）來相容兩種裝飾器語法。
* 建議對非正整數拋出 `ValueError`，對非整數型態（如 float）拋出 `TypeError`。
* 說明當被裝飾函式出錯時，不應計算時間，必須直接將例外原封不動 `raise` 向上傳遞。

### 3. 我改了什麼 (Crucial Adjustments)
> ⚠️ **這部分是評審重點，請依據你實際寫 code 時的改動填寫。以下為思維範例：**
* **規格收斂**：AI 本來只考慮到 `repeat < 1` 的 `ValueError`，我主動追加了型態檢查（傳入 `2.5` 或字串時要拋出 `TypeError`），讓邊界防禦更嚴密。
* **測試調整**：在討論 Edge cases 時，我決定在測試案例中加入「Method 裝飾測試」，確保 `self` 參數傳遞不會被裝飾器吃掉，這部分是 AI 一開始沒主動寫進基礎範例的。