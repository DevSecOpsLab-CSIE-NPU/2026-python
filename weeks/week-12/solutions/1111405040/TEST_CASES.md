# Week 12 測試案例說明

## QUESTION-10812
1. 一般可解案例：`40 20`，預期輸出 `30 10`
2. 分差大於總分：`20 40`，預期輸出 `impossible`
3. 和與差奇偶不符：`41 20`，預期輸出 `impossible`
4. 多筆測資混合輸入，確認整體格式正確

## QUESTION-10908
1. 範例中心點 `(1, 2)`，最大正方形邊長為 `3`
2. 較大區域中心點 `(4, 6)`，最大正方形邊長為 `5`
3. 接近邊界或字符不連續時，結果應回到 `1`
4. 使用題目範例驗證整體輸出

## QUESTION-10922
1. `9` 應為 9 的倍數，9-degree 為 `1`
2. `999999999` 應為 9 的倍數，9-degree 為 `2`
3. `12345` 不是 9 的倍數
4. 多筆輸入直到 `0`，確認輸出句型正確

## QUESTION-10929
1. `121` 是 11 的倍數
2. `123456` 不是 11 的倍數
3. 較長整數字串也能正確判斷
4. 多筆輸入直到 `0`，確認輸出句型正確

## QUESTION-10931
1. `1` 的二進位為 `1`，Parity 為 `1`
2. `21` 的二進位為 `10101`，Parity 為 `3`
3. 較多位數的二進位輸出格式應正確
4. 多筆輸入直到 `0`，確認輸出句型正確
