# Week 12 AI 使用紀錄

## 1. 提問與諮詢
- 如何處理高達 1000 位的數字而不用 `int()` 轉型？
- 在 2D 網格中心擴張時，如何最快判斷邊界內的所有字元相同？

## 2. 採用的建議
- 使用逐位取餘數 (Modular Arithmetic) 來處理 11 的倍數判斷。
- 使用 `all()` 搭配生成式簡化 2D 區域的字元檢查。

## 3. 錯誤修正紀錄
- **問題**: 10812 (Beat the Spread) 的測試在 `(s+d)%2 != 0` 時未正確輸出 `impossible`。
- **修正**: 加入了對奇偶性的檢查以及 `s < d` 的邊界檢查。
