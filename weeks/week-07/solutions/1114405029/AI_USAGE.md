# AI 協作紀錄 (AI_USAGE.md)

### 👤 學生資訊
* **學號**: 1114405029
* **姓名**: 林佳宏

### 🤖 AI 工具使用說明
本次作業主要使用 **Gemini 3 Flash** 進行協作。

### ✅ 協作內容摘要

1. **環境與路徑除錯**:
   * 協助解決 PowerShell 指令路徑錯誤（因資料夾層級包含 `2026-python` 而導致 `cd` 失敗）。
   * 指導使用 `Tab` 鍵補齊路徑與確認檔案絕對路徑。

2. **TDD 測試架構建立**:
   * 提供 `test_chibi.py` 的完整測試框架，包含資料讀取、戰鬥邏輯、重構驗證等共 16 個測試案例，確保符合 TDD 開發流程。
   * 協助處理跨資料夾匯入模組時的 `sys.path` 路徑問題。

3. **核心邏輯偵錯 (Debugging)**:
   * **ImportError**: 解決測試檔找不到 `chibi_battle.py` 的問題。
   * **KeyError**: 修正 `load_generals` 函式，處理 `generals.txt` 中的空行與 `EOF` 邏輯，確保武將資料正確載入。
   * **IndexError**: 修正因資料未成功讀取導致的清單索引超出範圍問題。

4. **課程知識整合與重構**:
   * 指導如何使用 `namedtuple` 封裝武將屬性。
   * 整合 `Counter` 進行傷害排名統計，以及使用 `defaultdict` 追蹤兵力損失。
   * 提供 Stage 3 的 ASCII 視覺化統計報表重構建議。

### ⚠️ 自我聲明
本作業之核心戰鬥邏輯、檔案讀取流程及 TDD 測試流程均經過本人理解與實際執行測試。AI 工具主要用於輔助解決環境設定、語法報錯排解及測試案例之生成，最終程式碼皆已於本地環境通過測試。