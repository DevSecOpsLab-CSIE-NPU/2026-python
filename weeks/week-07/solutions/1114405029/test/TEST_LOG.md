# 測試日誌 (TEST_LOG.md)

### 🔴 RED Stage (初次測試失敗)
* **時間**: 2026-04-11
* **結果**: FAILED (errors=16)
* **錯誤原因**: 
    1. 尚未在 `chibi_battle.py` 中實作任何類別與方法，導致測試腳本匯入時發生 `AttributeError`。
    2. 由於核心邏輯檔案為空，所有關於資料讀取與戰鬥模擬的測試案例均無法執行。

### 🟢 GREEN Stage (實作並通過測試)
* **時間**: 2026-04-11
* **指令**: `python test_chibi.py`
* **結果**: **OK (Ran 16 tests)**
* **偵錯與修復過程**:
    1. **路徑問題排除**: 修正測試腳本中的 `sys.path`，解決跨資料夾匯入 `chibi_battle` 模組失敗的問題。
    2. **檔案讀取優化**: 發現 `generals.txt` 讀取時會因空行導致中斷，重新實作 `load_generals` 邏輯，加入 `strip()` 與空行跳過機制，並精確處理 `EOF` 標記，成功載入 9 位武將。
    3. **邏輯除錯**: 解決 `KeyError` 問題，確保在計算傷害前武將資料已完整載入字典。
    4. **公式實作**: 完成 `max(1, ATK - DEF)` 傷害計算公式，並通過保底傷害測試。

### 🔵 REFACTOR Stage (重構與優化)
* **重構內容**: 
    1. **視覺化報表**: 實作 `print_report` 方法，利用 ASCII 字元（如 `█`, `░`）與 `center()` 函數製作精美的傷害統計長條圖。
    2. **程式碼美化**: 移除開發期間用於除錯的 `print([Debug])` 訊息，使終端機輸出符合專業作業要求。
    3. **結構優化**: 確保 `namedtuple` 的使用貫穿整個引擎，提升程式碼可讀性。
* **回歸測試**: 
    * 執行 `python test_chibi.py`，確認 16 個測試案例依然全數通過 (**OK**)。
    * 驗證重構後的輸出格式正確，且未破壞原有的戰鬥數據準確性。