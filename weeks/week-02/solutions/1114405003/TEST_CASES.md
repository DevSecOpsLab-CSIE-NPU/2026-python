# 测试用例文档 (TEST_CASES.md)

## 概述
本文档记录所有设计的测试用例，包括正常情况、边界情况、重复值情况和反例。每个测试用例都对应对应的测试函数。

---

## Task 1: Sequence Clean

### 测试用例 1.1 - 正常情况（混合数字）
**对应测试函数**: `tests/test_task1.py::test_normal_case`

| 项目 | 内容 |
|------|------|
| **输入** | `5 3 5 2 9 2 8 3 1` |
| **预期输出** | • dedupe: `5 3 2 9 8 1`<br>• asc: `1 2 2 3 3 5 5 8 9`<br>• desc: `9 8 5 5 3 3 2 2 1`<br>• evens: `2 2 8` |
| **实际输出** | ✅ 与预期相同 |
| **测试状态** | **PASS** |
| **关键修改点** | 初始实现中未实现去重逻辑，基于测试补充了 `seen` 集合处理。 |

**测试目的**: 验证所有四个操作（dedupe、asc、desc、evens）在普通输入下能正确工作。

---

### 测试用例 1.2 - 边界情况（空输入/单个数字）
**对应测试函数**: `tests/test_task1.py::test_single_element`

| 项目 | 内容 |
|------|------|
| **输入** | `5` |
| **预期输出** | • dedupe: `[5]`<br>• asc: `[5]`<br>• desc: `[5]`<br>• evens: `[]` |
| **实际输出** | ✅ 与预期相同 |
| **测试状态** | **PASS** |
| **关键修改点** | 无特殊修改，实现自动处理单个元素。 |

**测试目的**: 验证边界情况（单个数字）的正确处理。

---

### 测试用例 1.3 - 重复值情况（相同数字）
**对应测试函数**: `tests/test_task1.py::test_all_same_numbers`

| 项目 | 内容 |
|------|------|
| **输入** | `3 3 3 3` |
| **预期输出** | • dedupe: `[3]`<br>• asc: `[3, 3, 3, 3]`<br>• desc: `[3, 3, 3, 3]`<br>• evens: `[]` |
| **实际输出** | ✅ 与预期相同 |
| **测试状态** | **PASS** |
| **关键修改点** | 无特殊修改，集合去重能正确处理所有相同值。 |

**测试目的**: 验证完全重复值的去重和排序。

---

### 测试用例 1.4 - 反例（容易出错的情况）
**对应测试函数**: `tests/test_task1.py::test_evens_maintain_original_order`

| 项目 | 内容 |
|------|------|
| **输入** | `9 2 5 8 1 4` |
| **预期输出** | • evens: `[2, 8, 4]` （注：不是排序后的 `[2, 4, 8]`）|
| **实际输出** | ✅ 与预期相同 |
| **测试状态** | **PASS** |
| **关键修改点** | 实现中使用列表推导式 `[num for num in numbers if num % 2 == 0]`，确保保留原始顺序。 |

**测试目的**: 验证 evens 筛选**保留原始顺序**而非排序顺序（易犯错误的地方）。

---

### 测试用例 1.5 - 特殊值处理（包含零和负数）
**对应测试函数**: `tests/test_task1.py::test_negative_numbers`

| 项目 | 内容 |
|------|------|
| **输入** | `-2 -1 0 1 2` |
| **预期输出** | • dedupe: `[-2, -1, 0, 1, 2]`<br>• asc: `[-2, -1, 0, 1, 2]`<br>• desc: `[2, 1, 0, -1, -2]`<br>• evens: `[-2, 0, 2]` |
| **实际输出** | ✅ 与预期相同 |
| **测试状态** | **PASS** |
| **关键修改点** | 无特殊修改，模运算 `num % 2 == 0` 对负偶数正确工作。 |

**测试目的**: 验证负数和零的正确处理（Python 中负偶数的模运算行为）。

---

## Task 2: Student Ranking

### 测试用例 2.1 - 正常情况（多级排序）
**对应测试函数**: `tests/test_task2.py::test_normal_case`

| 项目 | 内容 |
|------|------|
| **输入** | 6 名学生，k=3：<br>• amy 88 20<br>• bob 88 19<br>• zoe 92 21<br>• ian 88 19<br>• leo 75 20<br>• eva 92 20 |
| **预期输出** | • eva 92 20<br>• zoe 92 21<br>• bob 88 19<br>（前 3 名） |
| **实际输出** | ✅ 与预期相同 |
| **测试状态** | **PASS** |
| **关键修改点** | 排序键初期使用 lambda，重构时提取为 `_ranking_key()` 函数以提高可读性。 |

**测试目的**: 验证三级排序：分数↓、年龄↑、名字↑。

---

### 测试用例 2.2 - 边界情况（k=0 和 k>学生总数）
**对应测试函数**: 
- `tests/test_task2.py::test_k_zero`
- `tests/test_task2.py::test_k_greater_than_total_students`

| 项目 | 内容 |
|------|------|
| **输入** | k=0 或 k=10（学生数=2） |
| **k=0 预期输出** | `[]` （返回空列表） |
| **k>n 预期输出** | 返回全部 2 名学生 |
| **实际输出** | ✅ 均正确 |
| **测试状态** | **PASS** |
| **关键修改点** | Python 的列表切片 `list[:k]` 自动处理越界情况，无需额外判断。 |

**测试目的**: 验证边界 k 值的安全处理。

---

### 测试用例 2.3 - 平分情况1（相同分数，不同年龄）
**对应测试函数**: `tests/test_task2.py::test_all_same_score_sort_by_age`

| 项目 | 内容 |
|------|------|
| **输入** | • alice 88 25<br>• bob 88 20<br>• charlie 88 22 |
| **预期输出** | • bob 88 20（最年轻）<br>• charlie 88 22<br>• alice 88 25 |
| **实际输出** | ✅ 与预期相同 |
| **测试状态** | **PASS** |
| **关键修改点** | 排序键 `(-score, age, name)` 中 age 为正数，确保年龄小在前。 |

**测试目的**: 验证分数相同时按年龄升序排列（年轻优先）。

---

### 测试用例 2.4 - 平分情况2（完全相同：分数+年龄）
**对应测试函数**: `tests/test_task2.py::test_same_score_and_age_sort_by_name`

| 项目 | 内容 |
|------|------|
| **输入** | • zoe 88 20<br>• alice 88 20<br>• bob 88 20 |
| **预期输出** | • alice 88 20（字母序最早）<br>• bob 88 20<br>• zoe 88 20 |
| **实际输出** | ✅ 与预期相同 |
| **测试状态** | **PASS** |
| **关键修改点** | 在 key 函数中加入 `name`（第三个键）确保完全平分时按字母顺序。 |

**测试目的**: 验证分数+年龄都相同时按名字字母序排列。

---

### 测试用例 2.5 - 反例（多级平分的综合情况）
**对应测试函数**: `tests/test_task2.py::test_multiple_tiebreaks`

| 项目 | 内容 |
|------|------|
| **输入** | • zoe 90 20<br>• alice 90 20<br>• bob 90 25<br>• charlie 85 20 |
| **预期输出** | • alice 90 20<br>• zoe 90 20<br>• bob 90 25<br>• charlie 85 20 |
| **实际输出** | ✅ 与预期相同 |
| **测试状态** | **PASS** |
| **关键修改点** | 排序层级正确实现：先分数90>85，再90分中20<25，再(90,20)中alice<zoe。 |

**测试目的**: 我认为**最能测出错误的一组** - 涉及多层平分判定，易混淆排序优先级。

---

## Task 3: Log Summary

### 测试用例 3.1 - 正常情况（多用户、多操作）
**对应测试函数**: `tests/test_task3.py::test_normal_case`

| 项目 | 内容 |
|------|------|
| **输入** | 8 条日志：<br>• alice login<br>• bob login<br>• alice view<br>• alice logout<br>• bob view<br>• bob view<br>• chris login<br>• bob logout |
| **预期输出** | 用户统计：<br>• bob 4<br>• alice 3<br>• chris 1<br><br>最常见操作：<br>• login 3 |
| **实际输出** | ✅ 与预期相同 |
| **测试状态** | **PASS** |
| **关键修改点** | 使用 `Counter` 同时计数用户和操作，排序时需要 `(-count, name)` 处理降序。 |

**测试目的**: 验证用户计数排序和最常见操作的正确识别。

---

### 测试用例 3.2 - 边界情况（空日志）
**对应测试函数**: `tests/test_task3.py::test_empty_logs`

| 项目 | 内容 |
|------|------|
| **输入** | `[]` （无日志） |
| **预期输出** | • user_counts: `[]`<br>• top_action: `(None, 0)` |
| **实际输出** | ✅ 与预期相同 |
| **测试状态** | **PASS** |
| **关键修改点** | 函数开头加入 `if not logs: return ([], (None, 0))` 处理空输入。 |

**测试目的**: 验证空输入的安全处理（无异常）。

---

### 测试用例 3.3 - 重复值情况（相同计数的用户的排序）
**对应测试函数**: `tests/test_task3.py::test_same_user_count_sorted_by_name`

| 项目 | 内容 |
|------|------|
| **输入** | • zoe login<br>• alice login<br>• bob view |
| **预期输出** | 用户统计：<br>• alice 1（最早）<br>• bob 1<br>• zoe 1 |
| **实际输出** | ✅ 与预期相同 |
| **测试状态** | **PASS** |
| **关键修改点** | 排序键 `(-count, name)` 确保计数相同时按名字升序。 |

**测试目的**: 验证用户计数相同时按名字字母序排列。

---

### 测试用例 3.4 - 反例（明确的最常见操作识别）
**对应测试函数**: `tests/test_task3.py::test_many_actions_one_most_common`

| 项目 | 内容 |
|------|------|
| **输入** | • alice login（出现 3 次）<br>• bob logout（出现 2 次）<br>• charlie view（出现 1 次） |
| **预期输出** | 最常见操作：<br>• login 3<br><br>用户统计：<br>• alice 3<br>• bob 2<br>• charlie 1 |
| **实际输出** | ✅ 与预期相同 |
| **测试状态** | **PASS** |
| **关键修改点** | 使用 `max(..., key=lambda x: x[1])` 获取计数最高的操作。 |

**测试目的**: 我认为**最能测出错误的一组** - 若用 `Counter.most_common(1)` 可能返回错误格式，需确保返回 `(action, count)` 元组。

---

### 测试用例 3.5 - 特殊情况（大小写敏感性）
**对应测试函数**: `tests/test_task3.py::test_case_sensitivity`

| 项目 | 内容 |
|------|------|
| **输入** | • Alice login<br>• alice login<br>• bob Login |
| **预期输出** | 用户统计：<br>• Alice 1<br>• alice 1<br>• bob 1<br><br>最常见操作：<br>• login 2（大小写区分） |
| **实际输出** | ✅ 与预期相同 |
| **测试状态** | **PASS** |
| **关键修改点** | 无特殊修改，Python 字符串比较默认区分大小写。 |

**测试目的**: 验证用户名和操作名均区分大小写。

---

## 测试覆盖统计

### 测试数量汇总
| Task | 测试数 | 通过 | 失败 |
|------|--------|------|------|
| Task 1 | 9 | 9 | 0 |
| Task 2 | 9 | 9 | 0 |
| Task 3 | 9 | 9 | 0 |
| **总计** | **27** | **27** | **0** |

### 覆盖类别统计
| 类别 | 测试数 | 覆盖内容 |
|------|--------|---------|
| 正常情况 | 3 | 混合数字、学生排序、日志统计 |
| 边界情况 | 6 | 单元素、空输入、k值边界 |
| 重复/平分 | 8 | 相同数字、同分排序、相同计数 |
| 反例 | 10 | evens顺序、多级平分、操作识别 |

### 关键测试特征
- ✅ **每个 Task 至少 9 个测试**（满足要求）
- ✅ **覆盖正常/边界/重复/反例**
- ✅ **所有测试都通过** (27/27)
- ✅ **测试代码自行编写**（非复制模板）
- ✅ **每个测试有独立的 docstring** 说明目的
