# AI_LOG

## AI 協作協議五個問題與回答

### 1. 函式簽名與回傳型態

我問 AI 什麼：

> 第三題 Base Digit Root 應該拆成哪些函式？每個函式的輸入與回傳型態是什麼？

AI 給了什麼：

> AI 建議拆成 `to_base_digits(n: int, base: int) -> list[int]`、`digit_root_in_base(n: int, base: int) -> int`、`solve(input_text: str, base: int) -> str`，並另外加入 `validate_base()` 與 `validate_number()` 做輸入檢查。

我改了什麼：

> 我採用函式拆分，讓進位轉換與數字根計算可以分開測試。這樣若答案錯誤，可以判斷是轉換錯還是反覆加總邏輯錯。

---

### 2. 輸入範圍與邊界條件

我問 AI 什麼：

> 第三題輸入是什麼？base 依照我的學號應該是多少？

AI 給了什麼：

> AI 說明輸入是多個十進位非負整數，可用空白或換行分隔。根據學號末兩碼 `14` 與個位數 `u = 4`，第三題基底為 `base = 5`。

我改了什麼：

> 我確認本題所有驗算都必須以 `base = 5` 為準，而不是直接套用題目範例中可能出現的 `base = 8`。因此我將主程式與測試中的常數設定為 `BASE = 5`。

---

### 3. 例外行為

我問 AI 什麼：

> 第三題遇到負數或非法 base 要怎麼處理？

AI 給了什麼：

> AI 建議 `n < 0` 時丟出 `ValueError`，`base < 2` 時也丟出 `ValueError`，因為進位基底至少要是 2。

我改了什麼：

> 我採用 `validate_number()` 與 `validate_base()`，並保留測試中的負數與非法 base 案例，讓錯誤輸入不會被靜默處理。

---

### 4. Edge Cases

我問 AI 什麼：

> 第三題測試應該涵蓋哪些 edge cases？

AI 給了什麼：

> AI 建議測試 `n = 0`、`n < base`、`n = base`、需要反覆加總的數字、base 的次方、多筆輸入、空白分隔輸入、負數例外與非法 base 例外。

我改了什麼：

> 我保留這些測試，並特別加入 `to_base_digits()` 的單元測試，因為本題的關鍵不是十進位 digit sum，而是先轉指定進位再加總。

---

### 5. 驗收標準

我問 AI 什麼：

> 第三題怎樣才算完成？

AI 給了什麼：

> AI 說明完成標準包含：使用 `BASE = 5`、正確轉換進位、反覆加總直到結果小於 base、多筆輸入逐行輸出、pytest 全部通過。

我改了什麼：

> 我用 `0 8 63` 進行人工驗算，確認在 `base = 5` 下輸出應為 `0 4 3`，不是 base = 8 範例的 `0 1 7`。最終測試結果為 `14 passed`。

---

## 我問 AI 什麼

「說明第三題任務內容。」

## AI 給了什麼

AI 說明第三題是任意進位的數字根，流程是將十進位非負整數轉成指定進位，將各位數字相加，若結果仍不是該進位的一位數就繼續重複。

## 我改了什麼

我確認本題不是單純十進位 digits sum，而是必須先依照個人參數轉成指定進位後再進行各位數字加總。

---

## 我問 AI 什麼

「第三題參數如何計算。」

## AI 給了什麼

AI 根據學號末兩碼 `14` 與個位數 `u = 4`，確認第三題進位基底為 `base = 5`。

## 我改了什麼

我將測試檔與主程式的常數設定為 `BASE = 5`，並且在驗算案例中都以五進位為準，而不是直接套用題目可能給的其他 base 範例。

---

## 我問 AI 什麼

「撰寫測試檔。」

## AI 給了什麼

AI 提供 `test_base_digit_root.py`，包含 `to_base_digits()`、`digit_root_in_base()`、`solve()` 的測試，並加入負數與非法 base 的例外測試。

## 我改了什麼

我保留測試中對正常案例與 edge cases 的覆蓋，並確認測試不只檢查最後輸出，也有針對進位轉換函式 `to_base_digits()` 做單元測試，方便定位錯誤。

---

## 我問 AI 什麼

「撰寫主程式。」

## AI 給了什麼

AI 提供 `base_digit_root.py`，包含 `BASE = 5`、`validate_base()`、`validate_number()`、`to_base_digits()`、`digit_root_in_base()`、`solve()` 與 `main()`。

## 我改了什麼

我檢查主程式的停止條件是 `while n >= base`，確認只有當數值已經小於 base 時才停止，符合「在該進位下剩一位數」的定義。

---

## 我問 AI 什麼

「輸入 0 8 63 預期輸出為?」

## AI 給了什麼

AI 以 `base = 5` 計算，說明 `0 -> 0`、`8 -> 13₅ -> 4`、`63 -> 223₅ -> 7 -> 12₅ -> 3`，所以預期輸出為 `0`、`4`、`3`。

## 我改了什麼

我確認不能直接使用題目中 base = 8 的範例輸出。由於我的個人參數是 `base = 5`，因此 `0 8 63` 的正確輸出應是 `0 4 3`，不是 `0 1 7`。

---

## 我問 AI 什麼

「撰寫 README.md AI_LOG.md 整理 TEST_LOG.md。」

## AI 給了什麼

AI 依照目前的主程式、測試檔、測試紀錄與 AI_LOG 範本，整理第三題的 README、AI_LOG 與 TEST_LOG。

## 我改了什麼

我保留測試紀錄中的第一次 Red 階段錯誤與第二次 Green 階段通過結果，並在文件中說明第一次錯誤是尚未找到 `base_digit_root.py` 導致，後續完成主程式後測試結果為 `14 passed`。
