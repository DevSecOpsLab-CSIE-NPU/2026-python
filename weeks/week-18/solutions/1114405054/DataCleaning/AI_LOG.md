# 第一題 AI LOG

## 基本資訊
- **學生**：1114405054 蔡珽州 (座號 54)
- **參數**：D=2

## 開工前檢查
1. 函式簽名：`clean_data(nums: list, d: int) -> list`
2. 輸入邊界：`1 <= n <= 10^5`，數字含負數與 0，讀到 `n = 0` 結束。
3. 例外處理：若篩選後為空，主程式必須印出大寫 `NONE`。
4. Edge Case：`[0, -2, -4, 4]` 應正確去重排序為 `[-4, -2, 0, 4]`；全奇數輸出 `[]`。
5. 驗收標準：去重(保留首次)->留偶數->升冪排序。數字間單一空白分隔。

## AI 協作紀錄（訪談摘要）

| 問題 | 學生回答 | checklist 狀態 |
|---|---|---|
| 函式簽名是什麼？ | `clean_data(nums: list, d: int) -> list` | ✅ |
| 輸入邊界？ | `1 <= n <= 10^5`，數字含負數與 0，讀到 `n = 0` 結束 | ✅ |
| 例外處理？ | 篩選後為空，主程式印出大寫 `NONE` | ✅ |
| Edge case？ | `[0, -2, -4, 4]` 應正確去重排序；全奇數輸出 `[]` | ✅ |
| 驗收標準？ | 去重(保留首次)->留偶數->升冪排序；數字間單一空白分隔 | ✅ |

紅燈 commit：`test: add DataCleaning test cases`
綠燈 commit：`feat: implement DataCleaning (dedupe, filter, sort)`
